{
    "private": true,
    "version": "1.0.0",
    "devDependencies": {
        "lerna": "^7.4.2"
    },
    "useWorkspaces": true,
    "packages": [
        "packages/client",
        "packages/server"
    ],
    "scripts": {
        "build": "npx lerna run build --parallel",
        "start": "npx lerna run start --parallel"
    }
}
